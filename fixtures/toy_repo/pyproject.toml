[project]
name = "toybug"
version = "0.0.1"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["toybug"]

[tool.pytest.ini_options]
pythonpath = ["."]
